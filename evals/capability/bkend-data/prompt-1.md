Create a data model with CRUD operations, filtering, and pagination using bkend.ai.
I am building a product catalog for an e-commerce application and need
the following data operations implemented:

1. A "products" table with name, description, price, category, tags, inStock status, and imageUrl
2. A "categories" table with name, slug, and parentCategory for nested categories
3. Create, read, update, and delete operations for both tables
4. Filtering products by category, price range, and inStock status
5. Text search across product name and description
6. Sorting by price (ascending/descending) and by creation date
7. Cursor-based or offset pagination with configurable page size
8. Proper indexing on frequently queried fields for performance

Use bkend MCP tools where available and REST API for data operations.
