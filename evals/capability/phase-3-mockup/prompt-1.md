Create a UI mockup for a task management dashboard application.
The dashboard should display tasks in a Kanban board layout with columns
for To Do, In Progress, In Review, and Done.
Each task card must show title, assignee avatar, priority indicator, and due date.
Include drag-and-drop visual affordances for moving tasks between columns.
Add a sidebar with project navigation, filters for priority and assignee,
and a top bar with search, notifications, and user profile menu.
Build the mockup using HTML and CSS that renders in a browser.
