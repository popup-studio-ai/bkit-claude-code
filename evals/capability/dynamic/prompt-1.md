Create a Next.js fullstack application for a team task tracker.
Use bkend.ai as the backend with authentication and data storage.
The app should support user registration, login, and session management.
Include a dashboard page showing tasks assigned to the logged-in user,
a task creation form with title, description, priority, and due date fields,
and API routes that handle CRUD operations for tasks.
Set up the project with TypeScript and Tailwind CSS.
