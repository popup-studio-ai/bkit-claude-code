Integrate frontend React components with a backend REST API for a user dashboard.
The dashboard displays user profile information, activity feed, and analytics charts.
Create an API client layer that handles authentication headers, request retries,
and response transformation.
Implement state management for the dashboard data with loading, error, and success states.
Handle optimistic updates for user profile edits and real-time polling for the activity feed.
Include proper error boundaries and fallback UI for failed API requests.
