Build a design system for a SaaS analytics platform.
Define design tokens for colors (primary, secondary, neutral, semantic), typography scale,
spacing scale, border radius values, and shadow elevation levels.
Create button components in three variants (primary, secondary, ghost) with
small, medium, and large sizes plus disabled and loading states.
Include an input field component, a card component, and a badge component.
Ensure all interactive components meet WCAG 2.1 AA accessibility standards.
Output the system as CSS custom properties with component class definitions.
